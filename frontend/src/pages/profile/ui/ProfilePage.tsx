/**
 * Placeholder — Task 12 replaces this with the real profile page (view/edit
 * the current user, avatar upload). It exists here only so `/profile` has a
 * route target and the router compiles.
 */
export function ProfilePage() {
  return <h1>Profile</h1>;
}
